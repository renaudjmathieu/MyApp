{
  "routes": [
    {
      "route": "/blog",
      "allowedRoles": ["authenticated"]
    },
    {
      "route": "/message/*",
      "allowedRoles": ["authenticated"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["*.{css,scss,js,png,gif,ico,jpg,svg}"]
  }
}